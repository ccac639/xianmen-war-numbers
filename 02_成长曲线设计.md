# 02 成长曲线设计

> 本文档定义《仙门战争》成长系统的**曲线形状**：宗门 4 级节奏、弟子成长、资源积累、声望扩张、难度递增。
>
> **本文档只定义曲线形状（凸/凹/S 型/指数/对数），不填任何具体数字。** 所有曲线参数（系数、指数、拐点位置）留待平衡阶段填入：
> ```
> data/config/curve.json
> ```
> 文中 `<待定>` 占位均为填数位置。

---

## 本文档范围

| 范围 | 是否覆盖 |
| --- | --- |
| 宗门 4 级（微弱 / 小型 / 中型 / 大型）的节奏设计 | ✅ |
| 各成长曲线的**形状选择**及理由（凸/凹/指数/对数/S 型） | ✅ |
| ASCII 曲线示意图 | ✅ |
| Demo 阶段的简化策略（先用线性近似） | ✅ |
| 给出任一具体数字 | ❌（违反用户约束） |

## Demo 阶段做哪部分

- **Demo 保留**：4 级宗门框架、弟子升级曲线、单条资源积累曲线、单条难度曲线。
- **Demo 简化**：所有正式曲线**先用线性近似**（一次函数），形状参数预留接口；正式版切换为正式曲线。
- **Demo 验证目标**：4 级升级节奏手感对，曲线切换后手感不崩。

---

## 一、宗门 4 级节奏总览

### 1.1 4 级定位

| 等级 | 名称 | 玩家体验定位 | 节奏目标 |
| --- | --- | --- | --- |
| 1 | 微弱 | 单据点、几名弟子、紧巴巴过日子 | 教学 + 首次升级 hook |
| 2 | 小型 | 多据点、产出转正、开始扩张 | 经济正向循环建立 |
| 3 | 中型 | 大地图竞争、对抗其他宗门 | 防御与进攻博弈 |
| 4 | 大型 | 争霸阶段、终局内容 | 长线目标 / sandbox |

### 1.2 升级节奏曲线（核心）

**设计目标**：每级升级所需"努力"递增，但**不能线性陡峭到劝退**，也不能太平缓失去目标感。

**形状选择：幂函数（凸函数），`effort(L) ∝ L^k_growth`，`k_growth > 1`**

```
effort
  ^
  |                          * (L=4 大型，长时间沉淀)
  |                     *
  |                *
  |           *
  |      *
  | * (L=1 微弱，快速达成)
  +---------------------------> 宗门等级 L
    1    2    3    4
```

**形状理由：**
- 凸函数让前期升级快（建立正反馈），后期慢（拉长终局体验）。
- 比"纯指数"温和，避免后期数值膨胀失控。
- 比"线性"有差异感，每级升完都更爽。

**Demo 简化**：先用线性 `effort(L) ∝ L`，正式版切幂函数。

**调参接口：**

| 参数 | 含义 | JSON 路径 |
| --- | --- | --- |
| `k_growth` | 升级努力指数（>1 凸，=1 线性） | `curve.json → curve.sect_upgrade.exponent` |
| `base_effort` | L=1 时的基础努力量 | `curve.json → curve.sect_upgrade.base` |
| `shape_switch` | 形状开关（`linear` / `power` / `exponential`） | `curve.json → curve.sect_upgrade.shape` |

### 1.3 4 级节奏分段（曲线轮廓）

```
游戏时间轴 ─────────────────────────────────────────►
            L1        L2         L3           L4
努力量  ┌──┐    ┌───┐     ┌────┐      ┌─────┐
       │短│    │中 │     │ 长 │      │ 长  │
       └──┘    └───┘     └────┘      └─────┘
乐趣密度 高       中         中          长线
```

| 阶段 | 期望时长占比 | 主要感受 |
| --- | --- | --- |
| L1→L2 | 短（占整体 `<待定>` 比例） | 快速上手、首胜爽点 |
| L2→L3 | 中 | 经济建立、扩张感 |
| L3→L4 | 长 | 对抗与策略博弈 |
| L4+ | 终局 | sandbox / 争霸 |

> 占比数值留待平衡阶段。

---

## 二、弟子成长曲线

### 2.1 弟子境界升级曲线（突破所需经验）

**形状选择：指数曲线（强凸）**，`breakthrough_cap(L) ∝ exp(k_br × L)`

```
经验
  ^                              * 高境界难如登天
  |                         *
  |                    *
  |               *
  |          *
  |     *
  | * 入门快
  +--------------------------------► 弟子境界 L
```

**形状理由：**
- 修仙题材天然适配"越高越难"，指数曲线强化"逆天而行"叙事感。
- 防止后期弟子无脑堆等级，逼玩家多线培养。

**Demo 简化**：先用线性 `breakthrough_cap(L) ∝ L`。

**调参接口：** `curve.json → curve.disciple_breakthrough.{shape, base, exponent}`

### 2.2 弟子战力随等级曲线

**形状选择：对数曲线（凹函数）**，`power(L) ∝ log(1 + k_pw × L)`

```
战力
  ^              ___________ 高境界战力边际递减
  |          ___/
  |       _/
  |     /
  |   /
  | /
  +--------------------------------► 弟子境界 L
```

**形状理由：**
- 凹函数让前期升级提升感强，后期靠装备/buff/配队而非单纯等级碾压。
- 与突破曲线的指数（凸）形成**剪刀差**：升级越来越难，但单级战力收益越来越小，鼓励玩家多弟子协同。

**Demo 简化**：线性 `power(L) ∝ L`。

**调参接口：** `curve.json → curve.disciple_power.{shape, base, coefficient}`

### 2.3 弟子寿命衰减曲线

**形状选择：分段函数**——
- 正常期：寿命恒定消耗（线性下降）
- 老化期：寿命加速消耗（凸函数下降）

```
剩余寿命
  ^ \                            \
  |  \                            \  老化期加速
  |   \                            \
  |    \                            \
  |     \____________________________\
  |       正常期线性                  老化期凸降
  +------------------------------------► 时间
```

**形状理由：**
- 给玩家"及时退役/复活"的压力，制造决策点。
- 老化期凸降触发玩家主动处理（复活 / 退场 / 突破延长）。

**Demo 简化**：全程线性下降，不区分老化期。

**调参接口：** `curve.json → curve.lifespan.{shape, aging_threshold, aging_rate}`

---

## 三、资源积累曲线

### 3.1 灵石持有量随时间曲线

**形状选择：S 型（sigmoid）**——

```
灵石持有
  ^                      ___________ 容量上限封顶
  |                    /
  |                  /
  |                /
  |              /     快速积累期
  |       _____/
  | _____/  缓启动期
  +--------------------------------► 游戏时间
```

**形状理由：**
- **缓启动**：初期产出渠道少，玩家需要建立基础设施。
- **快速积累**：基础设施成型后产能爆发，给玩家爽感。
- **封顶**：容量上限（`lingshi_cap`）+ 阶段性 sink 防止无限堆积。

**Demo 简化**：先用线性 `lingshi(t) ∝ t`，外加硬上限。

**调参接口：** `curve.json → curve.lingshi_accum.{shape, ramp_up, plateau_threshold}`

### 3.2 灵气（修炼 buff 资源）积累曲线

**形状选择：凹函数（边际递减）**，`lingqi_buff(x) ∝ sqrt(x)` 或 `log(1+x)`

```
修炼加速 buff
  ^            _________
  |        ___/
  |      /
  |    /
  |   /
  |  /
  | /
  +--------------------------------► 灵气投入 x
```

**形状理由：**
- 防止玩家堆灵气把修炼速度推到爆炸。
- 鼓励灵气分散投入多名弟子而非单点堆叠。

**Demo 简化**：buff 直接为 0（不启用灵气加速）。

**调参接口：** `curve.json → curve.lingqi_accel.{shape, coefficient}`

---

## 四、声望扩张曲线

### 4.1 声望积累曲线

**形状选择：阶梯型 + 每段内凹函数**——

```
声望
  ^                          ━━━━━━━━━ L4 里程碑
  |                    ━━━━━━
  |              ━━━━━━ L3 里程碑
  |        ━━━━━━
  |   ━━━━━ L2 里程碑
  | ━━ L1
  +--------------------------------► 游戏时间
```

**形状理由：**
- **阶梯**：声望是不可逆资源，每个里程碑对应宗门等级解锁，制造仪式感。
- **段内凹函数**：接近下一里程碑时声望获取减速（高难度任务更难做），自然产生卡顿感。

**Demo 简化**：只跑 L1→L2 一段，线性。

**调参接口：** `curve.json → curve.reputation.{shape, milestone_thresholds, segment_curve}`

### 4.2 声望与不可逆性

- 声望基本只能增加，失败少量扣减（有下限保护）。
- 占领据点 / 宗门升级给一次性里程碑奖励。
- 这条曲线本身不可逆，所以**斜率必须严格受控**——见 `03_经济循环.md` 防通胀章节。

---

## 五、难度递增曲线

### 5.1 任务难度曲线

**形状选择：阶梯式离散 + 每阶内线性**——

```
任务难度
  ^
  |                              ┌──── T_max
  |                         ┌────┘
  |                    ┌────┘
  |               ┌────┘
  |          ┌────┘
  |     ┌────┘
  | ┌─── T_min
  +--------------------------------► 解锁进度（声望/宗门等级）
```

**形状理由：**
- 离散难度档对应不同任务池，便于内容侧制作。
- 阶梯解锁让玩家有"解锁新内容"的爽点。

**Demo 简化**：固定 2~3 个难度档，档内线性。

**调参接口：** `curve.json → curve.quest_difficulty.{tiers, unlock_thresholds}`

### 5.2 任务风险/奖励随难度曲线

**风险：凸函数**（高难度死亡风险陡增）：

```
风险
  |                          / 死亡风险陡升
  |                        /
  |                      /
  |                    /
  |         _________/  低风险平缓
  | _______/
  +--------------------------------► 难度
```

**奖励：凹函数**（高难度奖励边际递减，防止堆难任务刷资源）：

```
奖励
  |           ___________
  |         /
  |        /
  |       /
  |      /
  | ____/
  +--------------------------------► 难度
```

**形状理由：**
- 风险凸 + 奖励凹 = **风险收益剪刀差**，逼迫玩家在中难度段寻找最优解。
- 防止"高战力无脑刷高难度"破坏经济。

**Demo 简化**：风险与奖励都线性，先验证闭环。

**调参接口：**
- 风险：`curve.json → curve.quest_risk.{shape, coefficient}`
- 奖励：`curve.json → curve.quest_reward.{shape, coefficient}`

### 5.3 据点防御随时间曲线

**形状选择：S 型反向**（建立期上升 → 稳定期平稳 → 被攻击期下降）：

```
占领度
  |            ━━━━━━━━━━━ 稳定期
  |          ╱              ╲
  |        ╱                  ╲
  |      ╱                      ╲ 被夺回
  |    ╱ 占领爬升                 ╲
  |  ╱                              ╲
  +------------------------------------► 时间
```

**形状理由：**
- 建立期：玩家投入产出爬升。
- 稳定期：净产出最高，玩家收益阶段。
- 衰退期：被其他宗门攻击时防御衰减，制造紧迫感。

**Demo 简化**：只跑"上升 + 稳定"两段，去掉衰退期。

**调参接口：** `curve.json → curve.stronghold_lifecycle.{rise_curve, stable_level, decay_curve}`

---

## 六、曲线对照总表

| 系统 | 正式版形状 | 性质 | Demo 简化 | JSON 路径 |
| --- | --- | --- | --- | --- |
| 宗门升级努力 | 幂函数 `L^k` | 凸 | 线性 | `curve.json → curve.sect_upgrade` |
| 弟子突破经验 | 指数 `e^(kL)` | 强凸 | 线性 | `curve.json → curve.disciple_breakthrough` |
| 弟子战力 | 对数 `log(1+kL)` | 凹 | 线性 | `curve.json → curve.disciple_power` |
| 弟子寿命 | 分段（线性+凸降） | 混合 | 全线性 | `curve.json → curve.lifespan` |
| 灵石积累 | S 型 sigmoid | S | 线性 + 上限 | `curve.json → curve.lingshi_accum` |
| 灵气加速 | 平方根 / 对数 | 凹 | 不启用 | `curve.json → curve.lingqi_accel` |
| 声望积累 | 阶梯 + 段内凹 | 混合 | 单段线性 | `curve.json → curve.reputation` |
| 任务难度 | 阶梯离散 | 离散 | 固定档 | `curve.json → curve.quest_difficulty` |
| 任务风险 | 凸 | 凸 | 线性 | `curve.json → curve.quest_risk` |
| 任务奖励 | 凹 | 凹 | 线性 | `curve.json → curve.quest_reward` |
| 据点生命周期 | 反 S 型 | S 变体 | 上升+稳定 | `curve.json → curve.stronghold_lifecycle` |

---

## 七、曲线切换架构（Demo → 正式版的关键）

为支持"先用线性、后切正式曲线"的演进策略，所有曲线统一通过形状开关调用：

```
value(L) = curve_dispatch(
    shape = curve.<system>.shape,     // "linear" | "power" | "exponential" | "log" | "sigmoid" | ...
    L,
    params = curve.<system>.{base, coefficient, exponent, ...}
)
```

**好处：**
1. Demo 阶段所有 `shape = "linear"`，平衡阶段逐条切换，无需改公式调用代码。
2. 单元测试可针对每种 shape 各跑一遍（见 `01_数值框架.md` §5.2）。
3. 回归快照在切换 shape 时自动失效，强制重测。

---

## 八、曲线设计的平衡原则

切换到正式曲线时，必须遵守以下**结构原则**（不含数字）：

1. **凸函数 vs 凹函数成对出现**——升级成本凸 + 收益凹 = 自然减速，防膨胀。
2. **每条曲线都有上界或衰减段**——禁止 `f(x) → ∞`，必须有 cap 或 plateau。
3. **曲线拐点错位**——升级曲线拐点 vs 资源曲线拐点不应重合，否则节奏感崩塌。
4. **Demo→正式切换只动 `curve.json`**——运行时代码不变，仅 JSON shape 字段变更。

---

## 九、与其他文档的关系

- **公式结构（变量、调参路径）** → 见 `01_数值框架.md`。
- **曲线对应的资源产出消耗闭环** → 见 `03_经济循环.md`。
- **本文件**只回答"这条曲线长什么样、为什么、Demo 怎么简化"。

---

**文档版本**：v0.1 框架稿（形状定义阶段）
**下一步**：进入 `03_经济循环.md`，把上述曲线接入产消闭环。
