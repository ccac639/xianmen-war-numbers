# 01 数值框架

> 本文档定义《仙门战争》全部数值系统的**结构骨架**：变量清单、公式形式、调参接口、平衡验证方法。
>
> **本文档只定义框架，不填任何具体数字。** 所有系数、阈值、初始值统一留待平衡阶段填入：
> ```
> data/config/balance.json
> ```
> 文档中以 `[min, max]`、`<待定>`、`TODO` 占位的字段，均为后续填数位置。

---

## 本文档范围

| 范围 | 是否覆盖 |
| --- | --- |
| 列出全部数值变量名（snake_case）、含义、单位、范围、所属 JSON 字段 | ✅ |
| 给出核心公式的**结构形式**（不含具体系数） | ✅ |
| 标明每个公式系数对应的 `data/config/*.json` 调参路径 | ✅ |
| 平衡验证方法论（单元测试 / 回归测试 / 压力测试） | ✅ |
| 给出任一具体数值 | ❌（违反用户约束） |

## Demo 阶段做哪部分

Demo 仅需跑通以下最小子集，正式版再补全：

- **Demo 保留**：宗门等级 1→2 升级、弟子战力与修炼速度、灵石主货币、贡献点内部循环、单个据点产出。
- **Demo 简化或砍掉**：声望系统（先用占位常量）、复活成本（固定公式即可）、特殊材料、多据点复杂防御。
- **Demo 公式策略**：所有曲线**先用线性近似**（详见 `02_成长曲线设计.md`），正式版替换为正式曲线。

---

## 一、全局约定

### 1.1 数值分层与 JSON 路径

所有数值常量按职责分层，集中存放在 `data/config/` 下：

| JSON 文件 | 职责 | 典型字段前缀 |
| --- | --- | --- |
| `data/config/balance.json` | 核心平衡系数（战力、修炼、成本、奖励基础值与乘子） | `combat.*` / `cultivation.*` / `cost.*` / `reward.*` |
| `data/config/sect.json` | 宗门等级阈值与规模上限 | `sect.level_thresholds` / `sect.scale_caps` |
| `data/config/disciple.json` | 弟子属性上下限、寿命与复活参数 | `disciple.*` |
| `data/config/resource.json` | 资源类型、初始量、容量上限 | `resource.*` |
| `data/config/quest.json` | 任务难度、风险、奖励系数 | `quest.*` |
| `data/config/stronghold.json` | 据点占领、产出、防御系数 | `stronghold.*` |
| `data/config/reputation.json` | 声望变化规则 | `reputation.*` |
| `data/config/curve.json` | 成长曲线形状参数（指数/对数/线性切换） | `curve.*` |

> 文档中所有 `→` 记法（如 `balance.json → combat.power_base`）指：调这个系数就去改这个 JSON 路径下的这个字段。

### 1.2 变量命名规范

- **运行时变量**（实例属性，存盘里）：`snake_case`，如 `current_power`、`lifespan_remaining`。
- **配置常量**（JSON 字段）：`snake_case`，加模块前缀避免冲突，如 `combat.power_base`。
- **占位范围**：`[min_x, max_x]` 表示待定上下界；占位系数一律写 `<待定>`。

---

## 二、数值变量清单（按模块）

每个变量给出：变量名、含义、单位、运行时取值范围（占位）、所在 JSON 字段、调参接口。

### 2.1 宗门数值（Sect）

| 变量名 | 含义 | 单位 | 取值范围 | 所在 JSON 字段 |
| --- | --- | --- | --- | --- |
| `sect_level` | 宗门等级（1=微弱 / 2=小型 / 3=中型 / 4=大型） | 级 | `[1, 4]` | 运行时存盘 |
| `sect_scale` | 宗门当前规模（弟子数上限索引） | 人 | `[min_scale, max_scale]` | `sect.json → sect.scale_caps[level]` |
| `sect_exp` | 宗门经验（升级进度） | 经验点 | `[0, sect_exp_cap]` | 运行时存盘 |
| `sect_exp_cap` | 当前等级经验上限 | 经验点 | `[min_cap, max_cap]` | `sect.json → sect.level_exp_caps[level]` |
| `upgrade_threshold_lingshi` | 升级所需灵石 | 灵石 | `[min_cost, max_cost]` | `sect.json → sect.level_upgrade_cost[level]` |
| `upgrade_threshold_reputation` | 升级所需声望 | 声望 | `[min_rep, max_rep]` | `sect.json → sect.level_upgrade_rep[level]` |
| `upgrade_threshold_strongholds` | 升级所需据点数 | 个 | `[min_sh, max_sh]` | `sect.json → sect.level_upgrade_sh[level]` |
| `disciple_cap_per_level` | 每级弟子容量上限 | 人 | `[min_cap, max_cap]` | `sect.json → sect.scale_caps[level]` |
| `sect_passive_income_rate` | 宗门等级产出加成系数 | 无量纲 | `[min_buff, max_buff]` | `balance.json → sect.income_buff_per_level` |

### 2.2 弟子数值（Disciple）

| 变量名 | 含义 | 单位 | 取值范围 | 所在 JSON 字段 |
| --- | --- | --- | --- | --- |
| `disciple_id` | 弟子唯一 ID | 整数 | `[1, +∞)` | 运行时存盘 |
| `disciple_level` | 弟子修炼境界等级 | 级 | `[1, max_disciple_level]` | 运行时存盘 |
| `base_power` | 弟子基础战力（境界决定） | 战力点 | `[min_bp, max_bp]` | `balance.json → combat.power_base` |
| `current_power` | 当前实际战力（含 buff/debuff） | 战力点 | 衍生量 | 运行时存盘 |
| `cultivation_speed` | 修炼速度（每秒境界经验） | 经验/秒 | `[min_cs, max_cs]` | `balance.json → cultivation.base_speed` |
| `cultivation_progress` | 当前境界经验进度 | 经验点 | `[0, breakthrough_cap]` | 运行时存盘 |
| `breakthrough_cap` | 突破到下一境界所需经验 | 经验点 | `[min_bc, max_bc]` | `disciple.json → disciple.breakthrough_caps[level]` |
| `lifespan_max` | 弟子寿命上限 | 秒 | `[min_life, max_life]` | `disciple.json → disciple.lifespan_max` |
| `lifespan_remaining` | 剩余寿命 | 秒 | `[0, lifespan_max]` | 运行时存盘 |
| `revive_count_used` | 已复活次数 | 次 | `[0, revive_count_max]` | 运行时存盘 |
| `revive_count_max` | 复活次数上限 | 次 | `[min_rev, max_rev]` | `disciple.json → disciple.revive_cap` |
| `contribution_points` | 弟子累计贡献点 | 点 | `[0, contribution_cap]` | 运行时存盘 |
| `loyalty` | 忠诚度（影响叛离概率） | 百分比 | `[0, 1]` | 运行时存盘 |
| `specialty_tag` | 弟子专精标签（影响任务适配） | 枚举 | `enum_specialty` | `disciple.json → disciple.specialties` |

### 2.3 资源数值（Resource）

| 变量名 | 含义 | 单位 | 取值范围 | 所在 JSON 字段 |
| --- | --- | --- | --- | --- |
| `lingshi` | 灵石（主货币） | 灵石 | `[0, lingshi_cap]` | `resource.json → resource.lingshi_cap` |
| `lingshi_cap` | 灵石容量上限 | 灵石 | `[min_cap, max_cap]` | `resource.json → resource.lingshi_cap` |
| `lingqi` | 灵气（修炼加速 buff 用） | 灵气点 | `[0, lingqi_cap]` | `resource.json → resource.lingqi_cap` |
| `lingqi_cap` | 灵气容量上限 | 灵气点 | `[min_cap, max_cap]` | `resource.json → resource.lingqi_cap` |
| `reputation` | 声望（不可逆积累） | 声望 | `[0, +∞)` | `resource.json → resource.reputation_init` |
| `contribution` | 宗门共享贡献池 | 点 | `[0, contribution_cap]` | `resource.json → resource.contribution_cap` |
| `special_material_id` | 特殊材料 ID（如天材地宝） | 枚举 | `enum_material` | `resource.json → resource.materials` |
| `special_material_count` | 特殊材料数量 | 个 | `[0, material_cap]` | 运行时存盘 |

### 2.4 任务数值（Quest）

| 变量名 | 含义 | 单位 | 取值范围 | 所在 JSON 字段 |
| --- | --- | --- | --- | --- |
| `quest_id` | 任务唯一 ID | 整数 | `[1, +∞)` | 运行时存盘 |
| `quest_difficulty` | 任务难度等级 | 级 | `[1, max_difficulty]` | `quest.json → quest.difficulty_tiers` |
| `quest_power_requirement` | 任务推荐战力门槛 | 战力点 | `[min_req, max_req]` | `quest.json → quest.power_req[difficulty]` |
| `quest_success_base` | 任务基础成功率 | 百分比 | `[0, 1]` | `balance.json → quest.success_base` |
| `quest_risk_injury` | 受伤风险系数 | 百分比 | `[0, 1]` | `quest.json → quest.risk_injury[difficulty]` |
| `quest_risk_death` | 死亡风险系数 | 百分比 | `[0, 1]` | `quest.json → quest.risk_death[difficulty]` |
| `quest_duration` | 任务时长 | 秒 | `[min_dur, max_dur]` | `quest.json → quest.duration[difficulty]` |
| `quest_reward_lingshi` | 任务灵石奖励基础值 | 灵石 | `[min_rw, max_rw]` | `balance.json → reward.quest_lingshi_base` |
| `quest_reward_exp` | 任务经验奖励基础值 | 经验点 | `[min_xp, max_xp]` | `balance.json → reward.quest_exp_base` |
| `quest_reward_reputation` | 任务声望奖励基础值 | 声望 | `[min_rep, max_rep]` | `balance.json → reward.quest_reputation_base` |
| `quest_invest_cost` | 任务投入灵石（提升成功率用） | 灵石 | `[0, invest_cap]` | `balance.json → quest.invest_unit_cost` |
| `quest_invest_success_gain` | 每单位投入带来的成功率增益 | 百分比/灵石 | `[min_g, max_g]` | `balance.json → quest.invest_success_gain` |

### 2.5 据点数值（Stronghold）

| 变量名 | 含义 | 单位 | 取值范围 | 所在 JSON 字段 |
| --- | --- | --- | --- | --- |
| `stronghold_id` | 据点唯一 ID | 整数 | `[1, +∞)` | 运行时存盘 |
| `occupation_level` | 占领度 | 百分比 | `[0, 1]` | 运行时存盘 |
| `occupation_rate` | 占领度每秒上升速率 | 1/秒 | `[min_or, max_or]` | `stronghold.json → stronghold.occupation_rate` |
| `lingshi_yield_rate` | 据点灵石产出速率 | 灵石/秒 | `[min_yr, max_yr]` | `stronghold.json → stronghold.lingshi_yield` |
| `lingqi_yield_rate` | 据点灵气产出速率 | 灵气/秒 | `[min_lyr, max_lyr]` | `stronghold.json → stronghold.lingqi_yield` |
| `defense_power` | 据点防御力（被夺回时抗性） | 防御点 | `[min_dp, max_dp]` | `stronghold.json → stronghold.defense_base` |
| `defense_decay_rate` | 防御衰减速率（被攻击时降低） | 1/秒 | `[min_dr, max_dr]` | `stronghold.json → stronghold.defense_decay` |
| `maintenance_cost` | 据点维护灵石成本 | 灵石/秒 | `[min_mc, max_mc]` | `stronghold.json → stronghold.maintenance_cost` |
| `garrison_power` | 驻守弟子总战力 | 战力点 | 衍生量 | 运行时存盘 |

---

## 三、核心公式结构（不含具体系数）

> 每条公式只写**形式**。所有 `k_*` 为待定系数，路径指向 `data/config/*.json`。

### 3.1 战力公式

```
current_power = base_power
              × (1 + buff_total)
              × realm_multiplier[disciple_level]
              × specialty_multiplier[specialty_tag, quest_type?]
              × loyalty_factor(loyalty)
              × sect_buff_per_level[sect_level]
```

**系数映射：**

| 符号 | 含义 | 调参路径 |
| --- | --- | --- |
| `base_power` | 基础战力 | `balance.json → combat.power_base` |
| `buff_total` | 所有临时 buff 之和（药品、阵法、装备等） | `balance.json → combat.buff_catalog` |
| `realm_multiplier[]` | 境界乘子表（按弟子等级索引） | `balance.json → combat.realm_multipliers` |
| `specialty_multiplier[]` | 专精适配乘子（含任务类型适配） | `balance.json → combat.specialty_matrix` |
| `loyalty_factor()` | 忠诚度→战力减成函数 | `balance.json → combat.loyalty_curve` |
| `sect_buff_per_level[]` | 宗门等级战力加成 | `balance.json → sect.power_buff_per_level` |

**Demo 简化**：`buff_total = 0`、`loyalty_factor = 1`、`specialty_multiplier = 1`，退化为 `current_power = base_power × realm_multiplier[disciple_level] × sect_buff[sect_level]`。

### 3.2 修炼速度公式

```
cultivation_speed = base_speed
                  × lingqi_acceleration(lingqi_available)
                  × sect_cultivation_buff[sect_level]
                  × talent_multiplier[disciple_talent]
                  × fatique_factor
```

每秒推进：`cultivation_progress += cultivation_speed × dt`

突破条件：`cultivation_progress ≥ breakthrough_cap[disciple_level]` 时升级。

**系数映射：**

| 符号 | 含义 | 调参路径 |
| --- | --- | --- |
| `base_speed` | 基础修炼速度 | `balance.json → cultivation.base_speed` |
| `lingqi_acceleration()` | 灵气加速函数（凹函数，详见曲线文档） | `balance.json → cultivation.lingqi_accel_curve` |
| `sect_cultivation_buff[]` | 宗门修炼加成表 | `balance.json → cultivation.sect_buff` |
| `talent_multiplier[]` | 弟子天赋乘子 | `disciple.json → disciple.talent_table` |
| `fatique_factor` | 疲劳衰减因子（连续任务后降低） | `balance.json → cultivation.fatique_curve` |

**Demo 简化**：去掉 `lingqi_acceleration` 和 `fatique_factor`，退化为线性。

### 3.3 任务成功率公式

```
success_rate = quest_success_base
             + power_advantage_factor(current_power, quest_power_requirement)
             + invest_bonus(quest_invest_cost × quest_invest_success_gain)
             - penalty_low_loyalty
```

夹紧：`success_rate = clamp(success_rate, 0, success_rate_cap)`

其中：

```
power_advantage_factor = k_power × ln(1 + current_power / quest_power_requirement)
```

**系数映射：**

| 符号 | 含义 | 调参路径 |
| --- | --- | --- |
| `quest_success_base` | 基础成功率 | `balance.json → quest.success_base` |
| `k_power` | 战力优势系数 | `balance.json → quest.power_advantage_k` |
| `quest_invest_success_gain` | 投入增益系数 | `balance.json → quest.invest_success_gain` |
| `success_rate_cap` | 成功率上限 | `balance.json → quest.success_cap` |
| `penalty_low_loyalty` | 低忠诚惩罚 | `balance.json → quest.loyalty_penalty` |

**Demo 简化**：`power_advantage_factor` 用线性 `k_power × (current_power - quest_power_requirement)` 近似。

### 3.4 复活成本公式

```
revive_cost_lingshi = revive_base_cost
                    × revive_cost_growth[revive_count_used]
                    × disciple_value_factor(disciple_level)
```

约束：`revive_count_used < revive_count_max` 才允许复活。

**系数映射：**

| 符号 | 含义 | 调参路径 |
| --- | --- | --- |
| `revive_base_cost` | 基础复活成本 | `balance.json → cost.revive_base` |
| `revive_cost_growth[]` | 复活次数成本增长系数表 | `balance.json → cost.revive_growth_table` |
| `disciple_value_factor()` | 弟子等级价值放大 | `balance.json → cost.disciple_value_curve` |

**Demo 简化**：固定公式 `revive_base_cost × (1 + revive_count_used)`。

### 3.5 宗门升级条件

升级触发条件（**全部**满足）：

```
sect_exp                ≥ sect_exp_cap[sect_level]
lingshi                 ≥ upgrade_threshold_lingshi[sect_level]
reputation              ≥ upgrade_threshold_reputation[sect_level]
occupied_strongholds    ≥ upgrade_threshold_strongholds[sect_level]
```

升级动作扣除相应资源，`sect_level += 1`，重置 `sect_exp`，刷新容量上限。

**调参路径：** `sect.json → sect.level_upgrade_cost[]` / `level_upgrade_rep[]` / `level_upgrade_sh[]` / `level_exp_caps[]`。

### 3.6 据点产出公式

```
lingshi_yield = lingshi_yield_rate
              × occupation_level
              × garrison_factor(garrison_power)
              × sect_yield_buff[sect_level]
              - maintenance_cost
```

**系数映射：**

| 符号 | 含义 | 调参路径 |
| --- | --- | --- |
| `lingshi_yield_rate` | 基础产出速率 | `stronghold.json → stronghold.lingshi_yield` |
| `occupation_level` | 当前占领度（0~1） | 运行时 |
| `garrison_factor()` | 驻军战力增益 | `stronghold.json → stronghold.garrison_curve` |
| `sect_yield_buff[]` | 宗门等级产出加成 | `balance.json → sect.income_buff_per_level` |
| `maintenance_cost` | 维护成本 | `stronghold.json → stronghold.maintenance_cost` |

**Demo 简化**：`garrison_factor = 1`，净产出 `= lingshi_yield_rate × occupation_level - maintenance_cost`。

### 3.7 声望变化公式

声望为**单向积累**资源（基本不可逆减少）：

```
reputation_delta = Σ quest_reputation_gain(success)
                 - Σ reputation_loss_on_failure
                 - Σ reputation_loss_on_sect_action
                 + stronghold_capture_bonus
                 + level_milestone_bonus
```

**约束：** `reputation ≥ 0`，失败扣声望但有下限；占领据点/宗门升级给一次性奖励。

**调参路径：** `reputation.json → reputation.*`（各分项系数）+ `balance.json → reward.quest_reputation_base`。

**Demo 简化**：只跑 `quest_reputation_gain` 和 `stronghold_capture_bonus` 两项。

---

## 四、调参接口总览（速查表）

| 想调什么 | 改哪个 JSON 路径 |
| --- | --- |
| 弟子基础战力 | `data/config/balance.json → combat.power_base` |
| 境界乘子曲线 | `data/config/balance.json → combat.realm_multipliers` |
| 修炼速度 | `data/config/balance.json → cultivation.base_speed` |
| 灵气加速曲线 | `data/config/balance.json → cultivation.lingqi_accel_curve` |
| 任务基础成功率 | `data/config/balance.json → quest.success_base` |
| 战力优势系数 | `data/config/balance.json → quest.power_advantage_k` |
| 任务投入增益 | `data/config/balance.json → quest.invest_success_gain` |
| 复活基础成本 | `data/config/balance.json → cost.revive_base` |
| 复活增长曲线 | `data/config/balance.json → cost.revive_growth_table` |
| 宗门升级阈值 | `data/config/sect.json → sect.level_upgrade_*.level` |
| 弟子寿命/复活上限 | `data/config/disciple.json → disciple.*` |
| 资源容量上限 | `data/config/resource.json → resource.*_cap` |
| 任务难度/风险/时长 | `data/config/quest.json → quest.*` |
| 据点产出/防御 | `data/config/stronghold.json → stronghold.*` |
| 声望分项系数 | `data/config/reputation.json → reputation.*` |
| 曲线形状开关 | `data/config/curve.json → curve.*` |

> **纪律**：禁止把魔法数字硬编码进 GDScript。任何常量必须可追溯到上表中的某条 JSON 路径。

---

## 五、平衡验证方法论

### 5.1 总体原则

1. **数据驱动**：所有系数走 JSON，单元测试只读 JSON 不依赖 GDScript 硬编码。
2. **不变量优先**：先把"绝对不会违反"的不变量写出来，再写具体数值断言。
3. **回归兜底**：每次改 `balance.json` 自动跑全量回归，防止某项调整引发雪崩。

### 5.2 单元测试编写方法

每个公式对应一个测试文件，验证**结构性质**而非具体数值。例：

| 测试名 | 验证性质（不含具体数字） | 通过条件 |
| --- | --- | --- |
| `test_power_monotonic_in_level` | 战力随弟子等级严格单调递增 | `power(L+1) > power(L)` for all L |
| `test_power_nonnegative` | 战力恒非负 | `current_power ≥ 0` |
| `test_success_rate_in_unit_interval` | 任务成功率落在 `[0, 1]` | `0 ≤ rate ≤ success_rate_cap` |
| `test_success_rate_monotonic_in_power` | 成功率随投入战力单调不减 | `∂rate/∂power ≥ 0` |
| `test_revive_cost_monotonic_in_count` | 复活成本随次数单调递增 | `cost(n+1) ≥ cost(n)` |
| `test_revive_cost_within_cap` | 复活达上限后拒绝 | `revive_count_used ≥ max ⇒ 拒绝` |
| `test_sect_upgrade_requires_all_conditions` | 升级需所有阈值同时满足 | 任一不满足 ⇒ 不升级 |
| `test_stronghold_yield_nonnegative_when_occupied` | 占领度满时净产出非负 | `yield > 0 when occupation=1` |
| `test_reputation_nonnegative` | 声望不低于 0 | `reputation ≥ 0` 恒成立 |
| `test_breakthrough_resets_progress` | 突破后进度归零 | `progress 升级后 == 0` |

> **写法**：每个测试注入一组占位 fixture（从 `data/config/*.json` 读取），断言性质成立。数值本身用 `skip` 或 `xfail` 标注待平衡阶段启用。

### 5.3 回归测试：黄金快照法

```
1. 平衡阶段产出"黄金快照" balance_golden.json + 一组黄金数值输出 snapshot.csv
2. 每次改动 balance.json 后跑全量公式
3. 与 snapshot.csv 比对：
   - 关键指标（战力、产能、升级时长）偏差 > tolerance ⇒ 失败
   - tolerance 由曲线文档定义（如 ±X%）
4. 失败时输出 diff 报告，确认是预期调整还是回归
```

防崩坏的关键不变量（必须在每次回归中通过）：

- 单位时间净灵石产出 ≤ 总 sink 容量（防止通胀爆炸）
- 任意等级升级时长在 `[min_playtime, max_playtime]` 期望区间
- 弟子战力上限不超过任一 boss 任务门槛的 `k_boss_overshoot` 倍（防过快碾压）

### 5.4 压力测试场景

构造极端输入，验证系统不崩：

| 场景 | 输入构造 | 预期行为 |
| --- | --- | --- |
| 极速爆兵 | 弟子数拉满 + 全任务并行 | 灵石 sink 触发，净产出不超过爆炸阈值 |
| 资源枯竭 | 灵石归零 + 多据点失守 | 任务拒绝执行，UI 优雅降级 |
| 长时挂机 | 时间 ×N 倍速 | 不产生 NaN/Inf，溢出有上限保护 |
| 高难度碾压 | 弟子战力 vs 低难度任务 | 成功率夹紧到 cap，奖励按规则衰减 |
| 多重复活 | 弟子反复死亡复活 | 达上限后强制拒绝，无内存泄漏 |
| 据点全失 | 所有据点占领度归零 | 灵石产出归零，宗门不升级，但存档不坏 |

### 5.5 Demo 阶段的最小验证集

Demo 只跑三条：

1. 弟子从 1 级修炼到 `breakthrough_cap` 时间在期望区间。
2. 单据点占领满后，灵石净产出于 0 以上（确保正向循环）。
3. 宗门 1→2 升级所需总灵石 = 据点产出 × 预期挂机时长（验证首个升级节奏）。

---

## 六、与其他文档的关系

- **曲线形状**（指数/对数/S 型等的具体形态）→ 见 `02_成长曲线设计.md`。
- **经济闭环与防通胀**（source/sink 平衡）→ 见 `03_经济循环.md`。
- **本文档**只定义"变量 + 公式结构 + 调参入口 + 验证方法"。

---

## 附：JSON 骨架示意（仅结构，不填数字）

> 仅为示意字段存在性，**禁止填具体数值**。平衡阶段填入。

```
data/config/balance.json
{
  "combat": {
    "power_base": <待定>,
    "realm_multipliers": [<待定>, ...],         // 索引=弟子等级
    "specialty_matrix": { <待定> },
    "loyalty_curve": <待定>,
    "buff_catalog": { <待定> }
  },
  "cultivation": {
    "base_speed": <待定>,
    "lingqi_accel_curve": <待定>,
    "sect_buff": [<待定>, ...],
    "fatique_curve": <待定>
  },
  "quest": {
    "success_base": <待定>,
    "power_advantage_k": <待定>,
    "invest_success_gain": <待定>,
    "success_cap": <待定>,
    "loyalty_penalty": <待定>
  },
  "reward": {
    "quest_lingshi_base": <待定>,
    "quest_exp_base": <待定>,
    "quest_reputation_base": <待定>
  },
  "cost": {
    "revive_base": <待定>,
    "revive_growth_table": [<待定>, ...],
    "disciple_value_curve": <待定>
  },
  "sect": {
    "income_buff_per_level": [<待定>, ...],
    "power_buff_per_level": [<待定>, ...]
  }
}
```

其余 `sect.json` / `disciple.json` / `resource.json` / `quest.json` / `stronghold.json` / `reputation.json` / `curve.json` 骨架同此结构，字段对应本文第二章变量清单。

---

**文档版本**：v0.1 框架稿
**下一步**：进入 `02_成长曲线设计.md` 定义曲线形状，进入 `03_经济循环.md` 定义产消闭环。
